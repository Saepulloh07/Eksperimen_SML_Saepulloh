name: HeartDiseaseModelling

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      model_name:
        type: str
        default: "all"
    command: |
      python modelling.py --model_name {model_name}
